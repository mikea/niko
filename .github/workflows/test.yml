name: Tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  BUILD_TYPE: Release

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4

    - name: Install dependecies
      run: sudo apt-get install just re2c libopenblas-dev

    - name: Debug
      run: just test

    - name: Release
      run: just release

    - name: Verify Clean Git Tree
      run: git diff --exit-code
